{
  "name": "@cael/os-light",
  "version": "0.0.0-phase0",
  "type": "module",
  "bin": {
    "cael-os-light": "dist/cli/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "fixtures"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:file": "vitest run --reporter=basic",
    "packs:install": "npm --prefix packs/p1-rbac ci && npm --prefix packs/p2-eligibility ci && npm --prefix packs/p5-vehicle-ownership ci && npm --prefix packs/p6-query-understanding ci && npm --prefix packs/p7-semantic-ranking ci && npm --prefix packs/p8-conversational-maps ci && npm --prefix packs/p9-autocomplete ci",
    "packs:eval": "npm --prefix packs/p1-rbac test && npm --prefix packs/p2-eligibility test && npm --prefix packs/p5-vehicle-ownership test && npm --prefix packs/p6-query-understanding test && npm --prefix packs/p7-semantic-ranking test && npm --prefix packs/p8-conversational-maps test && npm --prefix packs/p9-autocomplete test",
    "acceptance:install": "node scripts/acceptance/install-acceptance.mjs",
    "prepack": "npm run build",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
